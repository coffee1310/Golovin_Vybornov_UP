<Page x:Class="PropertyManagement.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Главная"
      FontFamily="Segoe UI">

    <Page.Resources>
        <!-- Стиль для карточек статистики -->
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D4C8B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="130"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Opacity="0.1" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для основных карточек -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D4C8B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Opacity="0.1" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль для кнопок быстрых действий -->
        <Style x:Key="QuickActionButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="180"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Background" Value="#67BA80"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="15">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5AA670"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#4D945D"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для кнопок вторичных действий -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F4E8D3"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#D4C8B3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E8DCC7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для DataGrid -->
        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D4C8B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F0F0F0"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>

        <!-- Стиль для текста статистики -->
        <Style x:Key="StatNumberStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#67BA80"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <Style x:Key="StatLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="None"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>

        <Style x:Key="StatIconStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="32"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Page.Resources>

    <Grid Background="#F4E8D3">
        <ScrollViewer>
            <StackPanel Margin="20">
                <!-- Заголовок страницы -->
                <Border Background="White" 
                        Padding="20"
                        BorderBrush="#D4C8B3" 
                        BorderThickness="1"
                        CornerRadius="8"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Добро пожаловать в систему управления заявками"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="#333333"
                                   Margin="0,0,0,5"/>

                        <TextBlock Text="Здесь вы можете управлять зданиями, квартирами, собственниками и заявками на ремонт"
                                   FontSize="14"
                                   Foreground="#666666"/>
                    </StackPanel>
                </Border>

                <!-- Статистика -->
                <Border Background="White" 
                        Padding="20"
                        BorderBrush="#D4C8B3" 
                        BorderThickness="1"
                        CornerRadius="8"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="📊 Статистика" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#333333"
                                   Margin="0,0,0,15"/>

                        <WrapPanel HorizontalAlignment="Center">
                            <!-- Карточка зданий -->
                            <Border Style="{StaticResource StatCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Text="🏢" 
                                               Style="{StaticResource StatIconStyle}"/>

                                    <TextBlock Grid.Row="1"
                                               x:Name="BuildingsCount" 
                                               Text="0" 
                                               Style="{StaticResource StatNumberStyle}"/>

                                    <TextBlock Grid.Row="2"
                                               Text="Здания" 
                                               Style="{StaticResource StatLabelStyle}"/>
                                </Grid>
                            </Border>

                            <!-- Карточка квартир -->
                            <Border Style="{StaticResource StatCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Text="🏠" 
                                               Style="{StaticResource StatIconStyle}"/>

                                    <TextBlock Grid.Row="1"
                                               x:Name="ApartmentsCount" 
                                               Text="0" 
                                               Style="{StaticResource StatNumberStyle}"/>

                                    <TextBlock Grid.Row="2"
                                               Text="Квартиры" 
                                               Style="{StaticResource StatLabelStyle}"/>
                                </Grid>
                            </Border>

                            <!-- Карточка сотрудников -->
                            <Border Style="{StaticResource StatCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Text="👥" 
                                               Style="{StaticResource StatIconStyle}"/>

                                    <TextBlock Grid.Row="1"
                                               x:Name="EmployeesCount" 
                                               Text="0" 
                                               Style="{StaticResource StatNumberStyle}"/>

                                    <TextBlock Grid.Row="2"
                                               Text="Сотрудники" 
                                               Style="{StaticResource StatLabelStyle}"/>
                                </Grid>
                            </Border>

                            <!-- Карточка заявок -->
                            <Border Style="{StaticResource StatCardStyle}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0"
                                               Text="🔧" 
                                               Style="{StaticResource StatIconStyle}"/>

                                    <TextBlock Grid.Row="1"
                                               x:Name="RequestsCount" 
                                               Text="0" 
                                               Style="{StaticResource StatNumberStyle}"/>

                                    <TextBlock Grid.Row="2"
                                               Text="Заявки" 
                                               Style="{StaticResource StatLabelStyle}"/>
                                </Grid>
                            </Border>
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <!-- Быстрые действия -->

                <!-- Последние заявки -->
                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" 
                                    Margin="0,0,0,15">
                            <TextBlock Text="📝" 
                                       FontSize="20"
                                       Margin="0,0,8,0"/>
                            <TextBlock Text="Последние заявки" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#333333"/>
                        </StackPanel>

                        <DataGrid x:Name="RecentRequestsGrid" 
                                  Style="{StaticResource DataGridStyle}"
                                  Height="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" 
                                                    Binding="{Binding Id}" 
                                                    Width="60"/>
                                <DataGridTextColumn Header="Тип" 
                                                    Binding="{Binding Type}" 
                                                    Width="120"/>
                                <DataGridTextColumn Header="Описание" 
                                                    Binding="{Binding Desc}" 
                                                    Width="*"/>
                                <DataGridTextColumn Header="Статус" 
                                                    Binding="{Binding State}" 
                                                    Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>

                        <Button Content="Показать все заявки" 
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,15,0,0"
                                HorizontalAlignment="Right"
                                Click="ViewAllRequests_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>